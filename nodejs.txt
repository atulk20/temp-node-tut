environment ro run JS outside browser


NPM
1)node package manager
2)reuse our own code in other projects
3)use code written by other developers and share our own solution with other developers
4)npm calls the resuable code a package or modules or dependencies

nodes modules is created when dependencies are installed

package json stores information about our projects
and it stores dependecies that project is using



GIT HUB

1)git init
2)git add .
3)git commit -m "firstmsg"
4)git remote add origin {link of repo}
5)git branch -m main // will be switched  to main branch //unecessary 
6)git checkout -b master
7)git push -u origin master// push to master



